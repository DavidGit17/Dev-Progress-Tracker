You are improving an existing mobile-first web app called “Dev Rebirth Tracker”. The core UI, layout, and basic functionality are already working well. Your task is to enhance functionality and fix specific issues without breaking the current design or structure.

IMPORTANT:
- Do NOT redesign the UI completely
- Do NOT change the existing theme (black, white, gray, Nothing-style UI)
- Only enhance logic, state handling, and add missing features
- Maintain clean, modular code

----------------------------------------

1. FIX NOTIFICATION SYSTEM

Current issue:
- Notifications are not triggering reliably
- No support for reminder intervals before task start

Required improvements:

A. Notification Permission
- Request notification permission on app load
- Handle denied / granted states properly

B. Custom Reminder System
- Each task should support multiple reminder intervals
- Example: 30, 15, 5 minutes before start
- Store this in task model:
  reminders: number[] (minutes before start)

C. Scheduling Logic
- When a task is created or app loads:
  - Schedule reminders using setTimeout (for now)
- Each reminder should trigger:
  Title: "Upcoming Task"
  Body: "Task <title> starts in X minutes"

D. Customization UI
- Allow user to:
  - Select predefined reminders (5, 10, 15, 30 mins)
  - Add custom reminder values
- Store per task

NOTE:
- This is browser-based, so service workers are optional for now
- Ensure system works while app is open

----------------------------------------

2. FIX TASK STATUS UPDATE ISSUE

Current issue:
- “Mark as Done” and “Miss” buttons do not update UI immediately
- Tasks only update after refresh

Root cause:
- Likely direct state mutation or improper state updates

Required fix:

A. Immutable State Updates
- Use proper state update logic (Zustand or equivalent)
- Replace task object instead of mutating

B. Add Status Field
Each task should have:
status: "pending" | "active" | "done" | "missed"

C. Update Actions
- Mark as Done → status = "done"
- Mark as Missed → status = "missed"

D. Immediate UI Update
- UI must react instantly without refresh

----------------------------------------

3. IMPROVE TASK VISUAL STATE (UX ENHANCEMENT)

Do NOT remove tasks when completed or missed.

Instead:

- Done:
  - Slight fade
  - Strikethrough text
- Missed:
  - Dimmed + subtle red/gray tone
- Active:
  - Highlighted

This helps maintain daily history visibility.

----------------------------------------

4. AUTO MARK MISSED TASKS

If:
- Current time > task end time
- AND task status is still "pending"

Then:
- Automatically mark task as "missed"

Run this check periodically (e.g., every minute)

----------------------------------------

5. ENSURE PERSISTENCE

- All updates (status, reminders, sessions) must persist
- Use localStorage or IndexedDB
- On reload:
  - Restore tasks
  - Re-schedule pending reminders

----------------------------------------

6. CODE QUALITY REQUIREMENTS

- Modular functions:
  - scheduleTaskReminders(task)
  - updateTaskStatus(taskId, status)
- Avoid duplicated logic
- Keep components clean and reusable

----------------------------------------

7. OPTIONAL (IF POSSIBLE)

- Add subtle animation when:
  - Task marked done
  - Task marked missed
- Smooth transitions only, no heavy effects

----------------------------------------

GOAL:
Fix broken behavior, improve reliability, and enhance usability while preserving the existing UI and structure. This is an upgrade, not a redesign.

Ensure everything feels responsive, intentional, and consistent with a disciplined developer workflow.